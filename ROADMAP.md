# 로드맵

학생 관리 시스템의 단계별 개발 계획입니다. 각 단계는 이전 단계의 기능을 기반으로 하며, 순서대로 진행하는 것을 권장합니다.

## 2주 데모 계획 (진행 중)

2주 뒤 목표는 실사용 투입이 아니라 **핵심 흐름이 동작하는 데모 시연**. 아래 순서로 압축 진행하며, 완료된 하위 Phase 항목과 연동된다.

- [x] 1~3일차: 로그인 (Google OAuth + 테스트용 아이디/비밀번호) — Phase 0 연장. `google_credentials` 연동(테스트 계정에 Google 계정 추가 연결)은 Calendar 연동 작업(8~10일차) 때 함께 구현 예정
- [x] 4~7일차: 학생 CRUD (Phase 1)
- [x] 8~10일차: 반 관리 + FullCalendar UI + Google Calendar 단방향 동기화 (Phase 3)
- [ ] 11~13일차: 성적/숙제 관리 (Phase 5 일부)
- [ ] 14일차: 통합 점검, 버그 수정, 데모 리허설

## Phase 0. 기반 구축

- [x] Git 초기화 및 저장소 설정
- [x] 백엔드 프로젝트 셋업 (Node.js + Express)
- [x] PostgreSQL 스키마 설계 (users, google_credentials, classes, students, grades, homework/homework_submissions)
- [x] 프론트-백엔드 연동 구조 확정 (Vite proxy, API 클라이언트, 환경변수)
- [x] 로그인 구현 (Google OAuth + 테스트용 아이디/비밀번호)

## Phase 1. 학생 관리 (핵심 기능)

- [x] 학생 등록/조회/수정/삭제 (CRUD) — 성별(필수)·학교명(선택) 포함, 반 배정은 반 관리 구현(8~10일차) 이후
- [x] 학생 목록 화면 (검색)
- [x] 학생 상세 화면
- [ ] 목록 정렬

## Phase 3. 반 관리

- [x] 수강반 생성/조회/수정/삭제
- [x] 반별 학생 배정
- [x] 캘린더 기반 반 일정 관리 — 요일별 반복 일정(`class_schedules`)을 FullCalendar 주간 시간표로 표시
- [x] Google Calendar 단방향 동기화 — 반 일정 생성/수정/삭제 시 담당 강사의 Google Calendar에 반복 이벤트 자동 반영(앱 → Calendar만, 역방향 없음)
- [x] 메인 대시보드 월간 달력 — 반 수업 일정 + Google Calendar 실제 이벤트 통합 조회
- [x] 대시보드 날짜별 자유 메모 — 수업 일정과 무관하게 강사가 텍스트로 남기는 일정(학교 시험 일정, 학생 특이사항 등), 학원 전체 공유

## Phase 4. 출결 관리

- [ ] 학생별 출결 상태 등록 (출석/지각/조퇴/결석 등 세분화)
- [ ] 출결 현황 조회 (학생별, 반별, 기간별)

## Phase 5. 성적/과제 관리

- [ ] 학교 성적(내신 등)은 별도 메뉴가 아니라 학생 상세 페이지(`student-detail-page.tsx`)에서 입력/조회
- [ ] 모의고사 관리 메뉴 신설 (학원 자체 시험 성적 입력/조회, 학생별·과목별)
- [ ] 과제 등록 및 제출/완료 현황 체크 — 기존 '성적/과제 관리' 메뉴는 '과제 관리'로 개편

## Phase 6. 수강료 관리

- [ ] 반별 수강료/교재비 청구
- [ ] 수납 처리 및 미납 현황 조회

## Phase 7. 상담 관리

- [ ] 학생별 상담 기록 등록/조회
- [ ] 상담 이력 타임라인

## Phase 8. AI 분석

- [ ] 성적 추이 및 취약 과목 분석
- [ ] 학생별 맞춤 코멘트/리포트 생성 (Claude API 연동)

## Phase 9. PDF 다운로드

- [ ] 학생 개인 성적표/리포트 PDF 출력 (성적 + AI 코멘트 포함)

## Phase 9-1. 테스트 및 보안 점검

- [x] 백엔드 API 회귀 테스트 (Vitest + supertest) — 인증 가드(401), 입력 검증(400), 존재하지 않는 리소스(404), SQL 인젝션/XSS 문자열 안전성, 학원 전체 공유 설계 검증. Express `app`을 `createApp()`으로 분리해 테스트 가능하게 구조 변경
- [x] 프론트 E2E 테스트 (Playwright) — 보호 라우트 리다이렉트, 로그인 성공/실패, 반 생성 시 목록·시간표 반영, 날짜별 메모 등록 플로우
- [x] 종합 보안 점검 1차 (타입 오류/의존성 취약점/시크릿 노출) — 결과: 즉시 수정 필요 항목 없음
  - 타입 오류 0건 (프론트/백엔드/테스트/E2E 전체)
  - `googleapis` → `gaxios` → `uuid` 전이 의존성 moderate 취약점 2건 — 직접 고칠 수 없는 전이 의존성이라 무시 가능으로 분류, `googleapis` 업데이트 시 자연 해소 예상
  - `.env` 커밋 이력 없음, 프론트에 노출되는 환경변수(`VITE_API_BASE_URL`, `VITE_GOOGLE_CLIENT_ID`)는 원래 공개되어야 하는 값이라 문제 없음
  - `backend/seed-test-user.mjs`에 테스트 계정(`admin`/`test1234`) 하드코딩 — 로컬 개발용 스크립트, 실수로 프로덕션 DB에 실행되지 않도록 배포 전 실행 금지 가드 추가 검토 필요 (미해결)
- [ ] `seed-test-user.mjs`에 프로덕션 환경 실행 방지 가드 추가
- [ ] 반/학생 CRUD 등 나머지 기능에 대한 회귀 테스트 확장 (현재는 반 일정+캘린더 연동 범위만 커버)

## Phase 10. 배포

- [ ] 백엔드 + PostgreSQL 배포 (Railway 또는 Render)
- [ ] 프론트엔드 배포 (Vercel)

## 향후 고려 기능

기획 단계에서 논의했으나 아직 범위에 포함되지 않은 아이디어:

- 소통 (학부모 1:다수 / 1:1 채팅) — WebSocket 없이 폴링 등 대안 방식 검토 필요
- 학부모 커뮤니케이션 로그
- 알림/리마인더 (숙제 마감, 시험 일정)
- 반 전체 현황 대시보드
- QR 코드 기반 출결 체크 — 학생 본인 인증 수단이 없어 보류. 도입 시 학생용 로그인/식별 체계부터 별도 기획 필요

## 참고: 다인 강사 체제 전환

원래 1인(강사 본인) 체제로 기획했으나, Google Calendar 연동을 계기로 다인 체제로 전환 확정.

- 사용자는 강사만(학부모/학생 로그인 없음), 데이터는 학원 전체 공유(강사별 접근 제한 없음)
- `role`(원장/강사) 구분은 스키마에 반영했으나, 실제 권한 제약(학생 등록은 원장만 등)은 미적용 — 데모 이후 필요 시 적용
